import { Firestore } from "firebase/firestore";

import { FirestoreAdapterType } from "./adapter.firestore.types";

export type FirebaseWebDBTypes = Firestore;
export type FirebaseWebAdapterTypes = FirestoreAdapterType;
