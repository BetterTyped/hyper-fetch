{
  "extends": "../../tsconfig.json",
  "compilerOptions": {
    "rootDir": "./src",
    "baseUrl": "./src",
    "paths": {
      // We set double browser adapters because this file is used only for the browser build
      "@browser-adapter": ["./http-adapter/http-adapter.browser.ts"],
      "@server-adapter": ["./http-adapter/http-adapter.browser.ts"]
    }
  }
}
