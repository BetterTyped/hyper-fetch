export const getSubmitLoadingEventKey = (key: string): string => {
  return `${key}-submit-loading-event`;
};
export const getSubmitDrainedEventKey = (key: string): string => {
  return `${key}-submit-drained-event`;
};
export const getSubmitQueueEventKey = (key: string): string => {
  return `${key}-submit-queue-event`;
};
