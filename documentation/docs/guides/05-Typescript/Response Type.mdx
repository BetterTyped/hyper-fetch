---
sidebar_position: 3
---

Response type defines the data that will be returned to us from the server

---

### Example

```ts
type ResponseType = Array<{
  name: string;
  email: string;
  age: number;
}>;

const getUsers = builder.createCommand<ResponseType>()({
  endpoint: "/users",
});
```
