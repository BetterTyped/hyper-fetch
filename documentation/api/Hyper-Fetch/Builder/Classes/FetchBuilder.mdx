import Admonition from "@theme/Admonition";
import CodeBlock from "@theme/CodeBlock";

<Admonition type="caution">This class is deprecated and might be removed soon.</Admonition>
<p style={{ color: "var(--ifm-footer-link-color)" }}>

API documentation for the `new FetchBuilder()` class.

</p>

## Details

<p>
  <code>Cache</code> class should be initialized per every command instance(not modified with params or queryParams).
  This way we create container which contains different requests to the same endpoint. With this segregation of data we
  can keep paginated data, filtered data, without overriding it between not related fetches. Key for interactions should
  be generated later in the hooks with getCommandKey util function, which joins the stringified values to create
  isolated space.
</p>

## Import

<CodeBlock className="language-tsx">{`import { FetchBuilder } from "@better-typed/hyper-fetch"`}</CodeBlock>

## Example

<CodeBlock className="language-tsx">
  {`import { sum } from "../src"
describe("blah", () => {
  it("works", () => {
    expect(sum(1, 1)).toEqual(2);
  });
});
`}
</CodeBlock>

## Parameters

<table className="api-docs-table">
  <thead>
    <tr>
      <th>Name</th>
      <th>Type</th>
      <th>Default</th>
      <th>Description</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>Name</td>
      <td>Type</td>
      <td>Default</td>
      <td>Description</td>
    </tr>
    <tr>
      <td>Name</td>
      <td>Type</td>
      <td>Default</td>
      <td>Description</td>
    </tr>
    <tr>
      <td>Name</td>
      <td>Type</td>
      <td>Default</td>
      <td>Description</td>
    </tr>
  </tbody>
</table>

## Methods

<div className="api-docs-method">

### `modifyHeaders()`

<CodeBlock style={{ padding: "10px", fontSize: "12px" }} className="language-tsx api-docs-small-preview">{`modifyHeaders(command: FetchCommand): Promise<FetchCommand>`}</CodeBlock>
Modify the headers of the command instance and returns promise. Key for interactions should be generated later in the hooks
with getCommandKey util function, which joins the stringified values to create isolated space.

</div>

### `setHeaders()`

<CodeBlock className="language-tsx api-docs-small-preview">{`setHeaders(command: FetchCommand): FetchCommand>`}</CodeBlock>

Set the headers of the command instance ## Additional links

<a href="#">Something</a>
