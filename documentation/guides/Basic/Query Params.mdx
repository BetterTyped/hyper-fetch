The use of query params is based on an internal parser. We can freely change its options and format.

---

### Custom query params format

We can setup custom config with instructions how to stingify the values. It can be setup on the builder with
`setQueryParamsConfig` method.

#### Available options:

{@import Hyper-Fetch QueryStringifyOptions returns}

---

### Setting query params

You can set query params by using the `setQueryParams` method on command. It's type can be configured on command
creation.

```tsx
type QueryParamsType = {
  search: string;
  sort: string;
};

const getUsers = builder.createCommand<Response, void, void, QueryParamsType>()({ endpoint: "users" });

// Setting the query params

getUsers.setQueryParams({ search: "John", sort: "age" });
```

```tsx
const getUsers = builder.createCommand<Response, void, void, string>()({ endpoint: "users" });

getUsers.setQueryParams("?search=John&sort=age");
```
